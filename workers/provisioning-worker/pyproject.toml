[project]
name = "idp-provisioning-worker"
version = "0.1.0"
description = "Provisioning worker for IDP"
requires-python = ">=3.11"
dependencies = [
    "celery",
    "redis",
    "boto3",
    "python-dotenv"
]