[project]
name = "idp-provisioning-worker"
version = "0.1.0"
description = "Provisioning worker for IDP"
requires-python = ">=3.11"
dependencies = [
    "celery",
    "redis",
    "sqlalchemy",
    "psycopg2-binary",
    "boto3",
    "python-dotenv"
]
